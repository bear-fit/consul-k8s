apiVersion: v1
kind: Service
metadata:
  name: proxy-connect-injector
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: proxy-connect-injector
  ports:
    - port: {{ .Values.connectInject.proxy.httpsPort | default 9443 }}
      targetPort: webhook